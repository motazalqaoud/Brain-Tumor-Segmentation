[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "brain-tumor-segmentation"
version = "0.2.0"
description = "Clinical-grade brain tumor segmentation from MRI scans using 3D Attention U-Net."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Motaz Alqaoud" }]
requires-python = ">=3.8"
dependencies = [
    "pydicom>=2.4.0",
    "nibabel>=5.0.0",
    "SimpleITK>=2.3.0",
    "torch>=2.0.0",
    "torchvision>=0.15.0",
    "matplotlib>=3.7.0",
    "numpy>=1.24.0",
    "scipy>=1.10.0",
    "scikit-image>=0.21.0",
    "tqdm>=4.65.0",
    "pandas>=2.0.0",
]

[project.optional-dependencies]
notebooks = ["jupyter>=1.0.0", "ipywidgets>=8.0.0"]

[project.urls]
Homepage = "https://github.com/motazalqaoud/Brain-Tumor-Segmentation"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]
